const invalid = {
  err: true,
  msg: 'Url is not valid',
};

const notExists = {
  err: true,
  msg: 'Object does not exists',
};

const unexpected = {
  err: true,
  msg: 'There was an error processing your request',
};

export {
  invalid,
  notExists,
  unexpected,
}
